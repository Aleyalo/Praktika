import 'package:flutter/material.dart';

class ColleaguesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Коллеги'), backgroundColor: Colors.yellow),
      body: Center(child: Text('Раздел коллег')),
    );
  }
}
